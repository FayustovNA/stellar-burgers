
// export const totalPricing = state => {
//     const {
//         maker: { otherIngredients, bun },
//     } = state;
//     return otherIngredients.reduce((acc, item) => acc + item.price, 0) + bun.reduce((acc, item) => acc + item.price * 2, 0);
// };